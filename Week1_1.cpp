# ========================================
# 📌 파이썬 설치 (설명만 - 실행 X)
# ========================================
# 보통 파이썬은 공식 웹사이트(https://www.python.org)에서 다운로드할 수 있습니다.
# 리눅스에서는 아래와 같은 명령어로 설치합니다.
# sudo apt update
# sudo apt install python3

# ========================================
# 📌 코랩 이용 (설명만 - 실행 X)
# ========================================
# 구글 코랩은 웹에서 파이썬을 실행할 수 있는 환경입니다.
# 구글 드라이브에 노트북 파일(.ipynb)을 저장하고 공유할 수 있습니다.
# 코드 실행은 셀마다 Shift + Enter로 실행합니다.

# ========================================
# ✅ 조건문
# ========================================

# ▶ 조건문 - 강의용 코드
print("조건문 - 강의용 예시")
score = 85

if score >= 90:
    print("A 학점입니다.")
elif score >= 80:
    print("B 학점입니다.")
else:
    print("C 학점 이하입니다.")

# ========================================
# 📝 조건문 과제
# ----------------------------------------
# 문제:
# 사용자로부터 점수(0~100 사이 정수)를 입력받아 다음 기준에 따라 학점을 출력하세요.
# - 90점 이상: A 학점
# - 80점 이상 90점 미만: B 학점
# - 70점 이상 80점 미만: C 학점
# - 70점 미만: 재시험 대상
# ========================================
    
print("\n조건문 - 과제용 예시")
score = int(input("점수를 입력하세요: "))

if score >= 90:
    print("A 학점입니다.")
elif score >= 80:
    print("B 학점입니다.")
elif score >= 70:
    print("C 학점입니다.")
else:
    print("재시험 대상입니다.")

# ========================================
# ✅ 반복문
# ========================================

# ▶ 반복문 - 강의용 코드
print("\n반복문 - 강의용 예시")
for i in range(5):
    print(f"{i + 1}번째 반복입니다.")

# ========================================
# 📝 반복문 과제
# ----------------------------------------
# 문제:
# 사용자로부터 양의 정수 N을 입력받고,
# 1부터 N까지의 모든 정수를 더한 값을 출력하세요.
# 예: N = 5 → 1 + 2 + 3 + 4 + 5 = 15
# ========================================
    
print("\n반복문 - 과제용 예시")
n = int(input("숫자 N을 입력하세요: "))
total = 0

for i in range(1, n + 1):
    total += i

print(f"1부터 {n}까지의 합은 {total}입니다.")

# ========================================
# ✅ 리스트
# ========================================

# ▶ 리스트 - 강의용 코드
print("\n리스트 - 강의용 예시")
fruits = ["사과", "바나나", "체리"]
print(fruits[0])  # 사과
print(fruits[1])  # 바나나
print(fruits[2])  # 체리
    
# ========================================
# 📝 리스트 과제
# ----------------------------------------
# 문제:
# 정수들로 이루어진 리스트가 주어졌을 때,
# 그 중 짝수만 골라서 출력하세요.
# ========================================
    
print("\n리스트 - 과제용 예시")
numbers = [10, 21, 32, 43, 54, 65]

for num in numbers:
    if num % 2 == 0:
        print(f"짝수: {num}")

# ========================================
# ✅ 디렉토리 구조 설명 (리눅스) - 설명 및 시뮬레이션
# ========================================

# ▶ 디렉토리 구조 - 강의용 설명
print("\n디렉토리 구조 - 강의용 예시")
print("""
/
├── home/        # 사용자 디렉토리
│   └── username/
├── etc/         # 설정 파일
├── bin/         # 실행 파일
├── usr/         # 사용자 설치 프로그램
│   ├── bin/
│   └── lib/
└── tmp/         # 임시 파일
""")

# ▶ 디렉토리 구조 - 과제용 코드 (리스트로 흉내)
print("\n디렉토리 구조 - 과제용 예시")
linux_dirs = {
    "/": ["home", "etc", "bin", "usr", "tmp"],
    "/home": ["username"],
    "/usr": ["bin", "lib"]
}

for parent, children in linux_dirs.items():
    print(f"{parent} 안에 있는 디렉토리: {', '.join(children)}")
